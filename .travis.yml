language: python

sudo: false

dist: trusty
group: edge

python:
  - "3.6"
  - "3.7"
  - "3.8"

os:
  - linux


install:
  - pip install --upgrade pip
  - pip install -e .
  - pip install pytest

script:
  - "python -m pytest ./medacy/tests/ -m 'not slow'"
